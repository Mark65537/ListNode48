# Описание задачи
Для типа «односвязанный неизменяемый список», определенного как </p>
  Class ListNode<T> {</p>
      Public readonly T Value;</p>
      Public readonly ListNode<T> Next;</p>
      …</p>
  }</p>
Написать две функции: замены элемента в списке и объединения двух списков.
# Описание проектов
ListNode48 - проект служит для удобной отладки методов и вывода результатов 
на консоль </p>
ListNodeTest - проект с тестами и реализации класса "ListNode" и методов в нем. 
Класс с тестами и списком находятся в одном файле, так как этого требует задача
